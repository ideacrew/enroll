---
before_script:
  - "rm -rf ./spec/vocabularies"
bundler_args: "--without production"
cache: bundler
dist: trusty
group: stable
language: ruby
os: linux
script:
  - "bundle exec rspec"
services:
  - mongodb
