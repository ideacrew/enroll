<thead>
  <tr>
    <th>Name</th>
    <th>Organization</th>
    <th> Signup </th>
    <th>&nbsp;</th>
  </tr>
</thead>

<tbody>
  <% @staff.each do |staff| %>
    <tr>
      <td><%= staff.first_name + staff.last_name %></td>
      <td><%= BrokerAgencyProfile.find(staff.broker_role.broker_agency_profile_id).legal_name %></td>
      <td><a href='#' data-broker='<%=staff.id%>' class='help_button'> Request Help </a></td>
   </tr>
  <% end %>
</tbody>