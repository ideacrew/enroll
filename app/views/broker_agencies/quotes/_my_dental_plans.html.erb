<div class="container-fluid">
    <div>
      <h4>Step 3 - Select reference plans for comparison using Roster Cost to Employer</h4>

      <h5>Showing <strong id="x-of-plans"><%= @dental_plans.count %></strong> of <strong><%= @dental_plans.count %></strong>  plans based on your selection criteria</h5>
      <div class="btn-group plan_buttons" data-toggle="buttons" style='max-height: 340px; overflow-y: scroll' id="quote-dental-plan-list">
        <% @dental_plans.each_with_index do |p,index| %>
        <label class="btn <%= "active" if params['plans'].try(:include?, p.id.to_s) %>" >
          <input name="dental_plans[]" <%= "checked" if params['dental_plans'].try(:include?, p.id.to_s) %> type="checkbox" autocomplete="off" value="<%= p.id %>">
          <%= p.name  + ' $'%><span class='employer_cost'>0</span></label>
        <% end %>
      </div>
    </div>
      <div class='btn btn-primary' id='DentalCostComparison'>Compare Dental Costs</div>
</div>