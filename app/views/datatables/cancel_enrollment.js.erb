//var $parent_row = $( ".table tbody tr:not(tr.child-row)" ).eq( <%= @row %> );if ( $('tr.child-row:visible').length > 0 ) {
  //$('tr.child-row:visible').remove();
//}
//if ( $parent_row.next().hasClass('child-row') ) {
//} else {
  //$parent_row.after('<tr class="child-row"><td colspan="100"><%= j render partial: "datatables/cancel_enrollment" %></td></tr>');
//}
//console.log("Here");
//window.alert("Waiting for more javascript!");
var containing_row = $("#<%= @row %>").closest('TR');
var $parent_row = containing_row;
//var $parent_row = $(".table:first > tbody > tr:not(tr.child-row)").eq(<%= @row %>);
if ( $('tr.child-row:visible').length > 0 ) {
 $('tr.child-row:visible').remove();
}

if ( $parent_row.next().hasClass('child-row') ) {
// Do Nothing
}
else {
 $parent_row.after('<tr class="child-row"><%= j render partial: "datatables/cancel_enrollment" %></tr>');
}
