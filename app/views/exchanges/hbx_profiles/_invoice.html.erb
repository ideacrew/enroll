<% flash.each do |key, value| %>
  <div class="alert alert-success"> <%= value %>
  <a href="#" data-dismiss="alert" class="close interaction-click-control-×">×</a>
  </div>
<% end %>
<h1>Employers</h1>
  
  <%=
  select_tag( "parent_agency", options_for_select(@parent_agencies,'name'),  class: "form-control", prompt:"FILTER BY DEPARTMENT", id: "department-filter",
  data: {
    remote: true,
    url: url_for(action: :employer_invoice)
  }
)
  
  %>
  <%= render_datatable(@datatable) %>
<script type="text/javascript" charset="utf-8">
  initializeDataTables();
</script>
