if ($("#broker_applicants_roster").length > 0) {
  $('#broker_applicants').html("<%= escape_javascript(render "applicants") %>");
}
else {
  set_tab_content("<%= escape_javascript render "index" %>")
  enableBrokerApplicantFilters();
}