<h1>Events</h1>
<p>
  <strong>Name:</strong>
  <%= @scheduled_event.event_name %>
</p>

<p>
  <strong>Time:</strong>
  <%= @time.to_datetime %>
</p>

<p>
  <strong>Recurring:</strong>
  <%= @scheduled_event.recurring_rules %>
</p>

<%= link_to 'Back', exchanges_scheduled_events_path %>

<%= link_to "Delete Event", exchanges_scheduled_event_path(@scheduled_event), method: :delete %>

<%= link_to "Edit", edit_exchanges_scheduled_event_path(@scheduled_event) %>
