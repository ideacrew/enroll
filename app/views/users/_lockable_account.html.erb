<% if user.locked? %>
  <%= link_to "Unlock User Account", lockable_user_path(user.id), remote: true, class: 'btn btn-default lock-confirm-btn' %>
<% else %>
  <%= link_to "Lock User Account", lockable_user_path(user.id), remote: true, class: 'btn btn-default lock-confirm-btn' %>
<% end %>
