<%= button_to l10n('devise.passwords.admin.send_reset_password_instructions'), user_account_confirm_reset_password_path(user_id: @user_id, account_id: @account_id, username: @username), method: :put, remote: true, class: 'btn btn-default lock-confirm-btn' %>