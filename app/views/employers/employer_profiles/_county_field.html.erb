<% if @single_option %>
  <%= options_for_select @counties, selected: @counties.first%>
<% else %>
  <%= options_for_select @counties, selected: @counties.first, disabled: @counties.first, required: true %>
<% end %>
