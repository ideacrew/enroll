<nav class="row">
  <ul>
    <li <%= menu_tab_class(active_tab, "home") %>>
      <%= link_to 'MY DC Health Link', employers_employer_profile_path(:employer_profile_id=>@employer_profile.id, :tab=>'home', :id => @employer_profile.id), "aria-expanded" => "true" %>
    </li>
    <li <%= menu_tab_class(active_tab, "employees") %>>
      <%= link_to 'Employees', employers_employer_profile_path(:employer_profile_id=>@employer_profile.id, :tab=>'employees', :id => @employer_profile.id), "aria-expanded" => "true" %>
    </li>
    <li <%= menu_tab_class(active_tab, "benefits") %>>
      <%= link_to 'Benefits', employers_employer_profile_path(:employer_profile_id=>@employer_profile.id, :tab=>'benefits', :id => @employer_profile.id), "aria-expanded" => "true" %>
    </li>
    <li <%= menu_tab_class(active_tab, "brokers") %>>
      <%= link_to 'Brokers', employers_employer_profile_path(:employer_profile_id=>@employer_profile.id, :tab=>'brokers', :id => @employer_profile.id), "aria-expanded" => "true" %>
    </li>
    <li <%= menu_tab_class(active_tab, "documents") %>>
      <%= link_to 'Documents', employers_employer_profile_path(:employer_profile_id=>@employer_profile.id, :tab=>'documents', :id => @employer_profile.id), "aria-expanded" => "true" %>
    </li>
    <% unless current_user.try(:has_broker_agency_staff_role?)%>
    <li <%= menu_tab_class(active_tab, "inbox") %>>
      <%= link_to(employers_employer_profile_path(:employer_profile_id=>@employer_profile.id, :tab=>'inbox', :id => @employer_profile.id), class: 'hidden-xs', "aria-expanded" => "true" ) do  %>
      Inbox
      <span class="badge message-unread"><%= @employer_profile.inbox.unread_messages.try(:count) || 0 %></span>

      <% end %>
      <%= link_to(employers_employer_profile_path(:employer_profile_id=>@employer_profile.id, :tab=>'inbox', :id => @employer_profile.id), class: 'visible-xs', "aria-expanded" => "true" ) do  %>
      <span class="glyphicon glyphicon-comment" aria-hidden="true"></span>
      <small>Inbox</small>
      <% end %>
    </li>
    <% end %>
  </ul>
  <div class="panel panel-default">
    <div class="panel-heading">
      <h4>My Account</h4>
      <%= link_to 'Update Business Info', employers_employer_profile_path(:employer_profile_id=>@employer_profile.id, :tab=>'profile', :id => @employer_profile.id) %>
    </div>
      <div class="panel-body">
        <div>My Employees: <span class="fte-count"><%= @fte_count.present? ? @fte_count:0 %></span></div>
      </div>
    </div>
</nav>
