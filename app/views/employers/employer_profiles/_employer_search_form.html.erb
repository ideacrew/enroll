<%= form_for @employer_profile, { :as => :employer_profile, :url => match_employers_employer_profiles_path, :remote => true } do |f| %>
  <%= render 'employers/employer_profiles/search_fields', f: f %>
<% end %>