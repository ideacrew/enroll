<div class="table-responsive table-override">
  <table class="table table-striped table-bordered border-override" style="table-layout: fixed;">
    <td class="confirm-terminate-wrapper show_confirm_<%= @census_employee.id %>" colspan="10" id="enter-date">
      <tr>
        <td class="confirm-terminate-wrapper show_confirm_<%= @census_employee.id %>"  colspan="10"  id="enter-date">
          <div class="confirm-terminate <%=pundit_class EmployerProfile, :updateable?%>">
            <h5 style="margin-top: 0; margin-bottom: 0;"><strong>Enter termination date to remove <%= name_to_listing(@census_employee) %></strong>
            </h5>
            <div class="pull-right" style="margin-top: 2px;">
              <i class="fa fa-calendar fa-lg"></i> <input class="text-center date-picker" placeholder="MM/DD/YYYY" type="text">
              <a class="delete_confirm terminate" href="javascript:;" id="remove_<%= @census_employee.id %>">
                Terminate Employee
              </a>
              <a class="terminate cancel" style="margin-right: 15px;">Cancel</a>
            </div>
          </div>
        </td>
      </tr>
    </td>
  </table>
</div>
