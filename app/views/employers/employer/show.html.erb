<h3>Employer Details </h3>

Name: <%= @employer.legal_name %> </br>
FEIN: <%= @employer.fein %> </br>
Entity Kind: <%= @employer.entity_kind %>

</br>
</br>

<%= link_to "Back", employers_employer_index_path %> &nbsp;&nbsp;
<%= link_to 'Destroy',  @employers_employer,  method: :delete, confirm: 'Are you sure?'  %>

<hr/>

<%= link_to "Add Employer Census Family", new_employers_employer_family_path(@employer), class: "btn btn-primary" %> &nbsp;&nbsp;

<h3>Employer Roster </h3>

<table border = "1">
  <tr>
    <th> Employee Name </th>
    <th> DOB </th>
    <th> Hire Date </th>
    <th>  </th>
  </tr>

<% @employer.employee_families.each do |family| %>
  <tr>
    <td> <%= link_to family.employee.first_name, "/employers/employer/#{family.id}/edit_family" %> </td>
    <td> <%= family.employee.dob %> </td>
    <td> <%= family.employee.hired_on %> </td>
    <td> <%= link_to "Delete", employers_employer_family_path(@employer.id, family.id), confirm: "Are you sure you want to delete this family?", method: :delete %> </td>
  </tr>
<% end %>
</table>