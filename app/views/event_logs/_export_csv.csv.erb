"Eligibility","Outcome","Performed By","Time"
<% @event_logs.each do |event_log| %>
  <%= event_log[:event_category] %>,<%= event_log.monitorable.event_name.match(/[^.]+\z/).to_s %>,<%= event_log[:account_username] %>,<%= event_log[:event_time] %>
<% end %>

