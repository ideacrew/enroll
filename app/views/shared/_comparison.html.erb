<div class="modal-body">
  <div id="printArea">
    <h3> Choose Plan - Compare Selected Plans </h3>
    <h5> Each plan is different. Make sure you understand the differences so you can find the right plan to meet your needs and budget. </h5>
    <h4 class="emphasize">Important: Please review SBC for any coinsurance costs.</h4>
    <%= render "shared/plan_shoppings/more_plan_details" %>
    <table class="table bordered", border = "1", style="font-size:11px;" >
      <tr>
        <th>Plans</th>
        <% qhps.each do |qhp|%>
          <th colspan="2"><%= display_carrier_logo(qhp.plan.carrier_profile.organization.legal_name, {width: 80}) %></th>
        <% end %>
      </tr>
      <tr>
        <th></th>
        <% qhps.each do |qhp|%>
          <th colspan="2"><%= qhp.plan_marketing_name %></th>
        <% end %>
      </tr>
      <tr>
        <th></th>
        <% qhps.each do |qhp|%>
          <th colspan="2"><span class="<%= qhp.plan.metal_level.humanize.downcase %>-icon"><%= qhp.plan.metal_level.humanize %></span> &nbsp;*&nbsp;
          <%= qhp.plan.plan_type.upcase %>
          </th>
        <% end %>
      </tr>
      <tr>
        <th></th>
        <% qhps.each do |qhp|%>
          <th colspan="2"><%=number_to_currency(qhp[:total_employee_cost]) + " / month" %></th>
        <% end %>
      </tr>
      <tr>
        <th></th>
        <% qhps.each do |qhp|%>
          <th colspan="2">
            <%= link_to("Choose Plan", thankyou_insured_plan_shopping_path(@hbx_enrollment, plan_id: qhp.plan.id, change_plan: "", market_kind: @market_kind, coverage_kind: @coverage_kind, enrollment_kind: ""), class: 'btn btn-primary plan-select') %> &nbsp;
          </th>
        <% end %>
      </tr>
      <tr>
        <th>Provider Network</th>
        <% qhps.each do |qhp|%>
          <th colspan="2"><%= qhp.plan.nationwide ? "Nationwide" : "DC-Metro" %></th>
        <% end %>
      </tr>
      <tr>
        <th>PLAN BENEFITS <br> (In Network)</th>
        <% qhps.each do |qhp|%>
          <th colspan="1"> CO-PAY </th>
          <th colspan="1"> COINSURANCE </th>
        <% end %>
      </tr>
      <% @visit_types.each do |visit_type| %>
        <tr>
          <td><%= visit_type %></td>
          <% qhps.each do |qhp|%>
            <% matching_benefit = qhp.qhp_benefits.detect { |qb| qb.benefit_type_code == visit_type } %>
            <% if matching_benefit %>
              <% deductible = matching_benefit.find_deductible %>
              <td  colspan="1"><%= deductible.present? ? deductible.copay_in_network_tier_1 : "N/A" %></td>
              <td  colspan="1"><%= deductible.present? ? deductible.co_insurance_in_network_tier_1 : "N/A" %></td>
            <% end %>
          <% end %>
        </tr>
      <%end%>
      <tr>
        <th></th>
        <% qhps.each do |qhp|%>
          <th colspan="2">
            <div>
              <% if qhp.plan.sbc_document.present? %>
                <% key, bucket = get_key_and_bucket(qhp.plan.sbc_document.identifier) %>
                  <%= link_to("  Summary of Benefits and Coverage", document_download_path(bucket, key) + "?content_type=application/pdf&filename=#{qhp.plan.name.gsub(/[^0-9a-z]/i,'')}.pdf&disposition=inline", class: "sbc_link", :target => "_blank") %>
              <% else %>
                <i class="fa fa-file-pdf-o fa-1x" style="position: relative; top: 7px;"></i>
                <div class="fa-icon-label" style="display: inline; font-size: 8px; position: relative; top: 5px;">Summary of Benefits and Coverage</div>
              <% end %>
            </div>
          </th>
        <% end %>
      </tr>
      <tr>
        <th></th>
        <% qhps.each do |qhp|%>
          <th colspan="2">
            <%= link_to("Choose Plan", thankyou_insured_plan_shopping_path(@hbx_enrollment, plan_id: qhp.plan.id, change_plan: "", market_kind: @market_kind, coverage_kind: @coverage_kind, enrollment_kind: ""), class: 'btn btn-primary plan-select') %> &nbsp;
          </th>
        <% end %>
      </tr>
    </table>
  </div>
</div>
<div class="modal-footer">
  <button type="button" class="btn btn-select" id='btnPrint'>Print</button>
  <%= link_to "Download", comparison_products_plans_path(market_kind: @market_kind, coverage_kind: @coverage_kind, standard_component_ids: @standard_component_ids, hbx_enrollment_id: @hbx_enrollment_id, active_year: @active_year, format: :csv), class: "btn btn-select" %>
  <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
</div>
