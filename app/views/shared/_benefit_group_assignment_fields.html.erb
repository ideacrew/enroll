<% benefit_groups = @employer_profile.plan_years.map(&:benefit_groups).try(:flatten) %>
<% default = @employer_profile.default_benefit_group %>
<%= f.select :benefit_group_id, options_from_collection_for_select(benefit_groups, 'id', 'title', (@census_employee.active_benefit_group_assignment.try(:benefit_group_id) || default.try(:id))), prompt: "SELECT BENEFIT GROUP"  %>