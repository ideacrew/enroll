<% if offers_nationwide_plans? %>
  <% if qhps.compact.map(&:plan).compact.map(&:nationwide).compact.uniq.include?(true) %> #TO DO: figure out why it was returning nil values to begin with on me_demo
    <tr>
      <th></th>
      <% qhps.each do |qhp|%>
        <th colspan="2">
          <% if qhp.plan.nationwide %>
            <%= link_to "DOCTOR DIRECTORY", qhp.plan.provider_directory_url, target: "_blank" %>
          <% end %>
        </th>
      <% end %>
    </tr>
  <% end %>
<% else%>
  <tr>
    <th></th>
    <% qhps.each do |qhp|%>
      <th colspan="2">
        <%= link_to "DOCTOR DIRECTORY", qhp.plan.provider_directory_url, target: "_blank" %>
      </th>
    <% end %>
  </tr>
<% end %>
