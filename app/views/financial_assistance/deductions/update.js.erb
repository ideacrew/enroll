stopEditingDeduction();

$('#<%= dom_id @deduction %>').html("<%= escape_javascript render(partial: 'deduction', locals: { deduction: @deduction, kind: @deduction.kind }) %>");

window.location.reload();