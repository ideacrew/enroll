%broker
  %id= broker_agency_profile.hbx_id
  %name= broker_agency_profile.legal_name
  %dba= broker_agency_profile.dba
  %website= broker_agency_profile.website if broker_agency_profile.website
  %is_active= broker_agency_profile.is_active if broker_agency_profile.is_active