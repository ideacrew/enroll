<table class="table table-hover table-striped" id="doc_table">
  <col width="150">
  <col width="120">
  <col width="120">
  <col width="100">
  <col width="100">
  <col width="100">
  <thead class="thead-default">
    <th>Family member</th>
    <th>Person verification status</th>
    <th>Verification type</th>
    <th>Verification Status</th>
    <th>Remaining time</th>
    <th>Documents</th>
  </thead>
  <tr>
    <td><%= member.person.first_name %> <%= member.person.last_name%></td>
    <td><%= show_consumer_role_state(member.person.consumer_role) %></td>
    <td colspan="3">
      <table class="table table-hover table-striped">
        <col width="180">
        <col width="140">
        <col width="140">
        <% member.person.verification_types.each do |verif_type| %>
          <tr>
            <td><%= verif_type %></td>
            <td>Verification status</td>
            <td><%= time_remaining_in_words(member.person.created_at) %></td>
          </tr>
        <% end %>
      </table>
    </td>
    <td>
      <%= link_to documents_detail_insured_families_path, remote: true do %>
          <i class="fa fa-plus-square-o fa-3x"></i>
      <% end %>
    </td>
  </tr>
    <!--<td colspan="3">-->
      <!--<table class="table table-condensed">-->
        <!--<col width="120">-->
        <!--<col width="150">-->
        <!--<col width="140">-->
        <!--<col width="100">-->
        <!--<% member.person.consumer_role.vlp_documents.each do |doc| %>-->
            <!--<tr>-->
              <!--<td><%= doc.subject %></td>-->
              <!--<td style="padding-left: 40px;">-->
                <!--<% if doc.identifier.present? %>-->
                    <!--<% key = doc.identifier.split('#').last %>-->
                    <!--<%= link_to("/insured/verification_documents/download/#{key}", class: "download") do %>-->
                        <!--<i class="fa fa-download fa-2x"></i>-->
                    <!--<% end %>-->
                <!--<% end %>-->
              <!--</td>-->
              <!--<td><%= doc.created_at if doc.identifier %><br></td>-->
              <!--<td><%= doc.status %><br></td>-->
            <!--</tr>-->
        <!--<% end %>-->
      <!--</table>-->
    <!--</td>-->

</table>

<div id="documents_detail" style="display:none;">

</div>

