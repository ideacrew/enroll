<% content_for :navigation do %>
    <%= render partial: 'ui-components/v1/navs/families_navigation' %>
<% end %>
<h1 class="no-buffer"><%= l10n("paper_application_upload") %></h1>
<div class="documents-tab">
  <% if @person && @person.resident_role %>
      <h4 class="starter"><%= l10n(".upload_paper_application_to_complete") %></h4>
        application for CoverAll.</h4>

        <%= render 'insured/families/coverall/coverall' %>
  <% end %>
</div>
