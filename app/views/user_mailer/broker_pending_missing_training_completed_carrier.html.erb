<%= l10n(
  "user_mailer.broker_pending_missing_training_completed_carrier.full_text",
  applicant_name: applicant_name,
  site_short_name: site_short_name,
  contact_center_name: contact_center_name,
  contact_center_phone_number: EnrollRegistry[:enroll_app].setting(:contact_center_short_number).item,
  training_link: EnrollRegistry[:broker_training_link].item
) %>

