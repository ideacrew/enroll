/*
 * This is a manifest file that'll be compiled into application.css, which will include all the files
 * listed below.
 *
 * Any CSS and SCSS file within this directory, lib/assets/stylesheets, vendor/assets/stylesheets,
 * or any plugin's vendor/assets/stylesheets directory can be referenced here using a relative path.
 *
 * You're free to add application-wide styles to this file and they'll appear at the bottom of the
 * compiled file so the styles you add here take precedence over styles defined in any styles
 * defined in the other CSS/SCSS files in this directory. It is generally better to create a new
 * file per style scope.
 *
 *= require main
 *= require jquery-ui
 *= require font-awesome
 *= require bootstrap-slider
 *= require bootstrap-multiselect
 *= require_self
 */


.module {
  .flex {
    display: flex;
    align-items: center;
    justify-content: center;
  }
}

 .registrations {
   .actions {
     span {
       display: block;
       text-align: center;
     }

   }
   .email-help {
     font-size: 14px;
   }

 }

.clear {
  clear: both;
}
.cna {
  cursor: not-allowed;
  display: none;
}
.dn {
  display: none;
}
 .starter {
   max-width: 660px;
 }
 .clear {
   clear: both;
 }
 .pz {
   padding: 0;
 }
 .bt-n {
   border-top: none;
 }
 .brz {
   border-radius: 0;
 }
 .br-default {
  border-radius: 6px;
 }

 .bn {
   border: none;
 }
 .b {
   border: solid 1px #d5d5d5;
 }
 .inv {
   visibility: hidden;
 }
 .m-center {
   margin: auto;
 }
 .dir {
  margin-top: 0;
}
.view-more {
  white-space: nowrap;
}
img {
 &.full-width {
   width: 100%;
 }
}
 .modal-body-swap {
   position: relative;
   padding: 15px;
   display: none;
 }
 .fa-center {
   &.fa-spinner {
     position: relative;
     left: 50%;
     margin-left: -14px;
     display: inline-block;
     padding: 15px 0;
   }
 }

// full width flash messenger style
.container > .col-xs-12 > .row > .alert.alert-notice {
  margin-top: 1%;
  margin-bottom: 1%;
}
 .panel-border {
   border: solid 1px #d5d5d5;
 }
 .empty-box {
   padding: 10px 10px;
   border: solid 1px #d5d5d5;
   &.important {
     background-color: #fcf8e3;

   }
   .fa {
     &.fa-flag {
       color: #F9BB33;
       border: solid 2px #F9BB33;
       border-radius: 200%;
       padding: 5px;
     }
   }
 }
 .content-bottom {
   padding-bottom: 45px;
 }
 .col-xs-12.ccs {
   border-right: none;
   padding-right: 0;
 }
 .ccs {
   padding-top: 13px;
   height: 70px;
   border-right: solid 1px #ddd;
   text-align: right;
   &+.col-xs-7 {
     padding: 10px 0 0;
     .top-menu {
       text-align: right;
       position: relative;
       li {
         padding: 0;
         .dark-grey {
           color: #5C5C5C;
           font-weight: 400;
           font-size: 12px;
           position: relative;
           top: -1px;
           display: inline-block;
           padding-right: 3px;
         }
       }
       h4 {
         font-size: 16px;
       }
     }
   }
 }


 #personal_info .tooltip-inner, #address_info .tooltip-inner, #confirm_plan .tooltip-inner, #choose-coverage .tooltip-inner, #offering_slider_header .tooltip-inner {

    max-width: 300px;
    /* If max-width does not work, try using width instead */
    width: 300px;
    text-align:left;
    text-transform: none;
 }
.consumer_roles {
  .fa {
    color: #007bc4;
  }
}
.fa-question-circle {
    color: #007bc4;
}


.asc::before {
  font-family: FontAwesome;
   content: "\f062";
   display: inline-block;
   padding-right: 3px;
   vertical-align: middle;
}

.desc::before {
  font-family: FontAwesome;
  content: "\f063";
  display: inline-block;
  padding-right: 3px;
  vertical-align: middle;
}
// phasing out out styles

// new radio style
.n-radio-group {
  .n-radio-row {
    background-color: #fff;
    border-left: 1px solid #b7b7b7;
    border-right: 1px solid #b7b7b7;
    border-bottom: 1px solid #b7b7b7;
    width: 100%;
    padding: 15px 10px;
    &:first-of-type {
      border-top: 1px solid #b7b7b7;
    }
    label.n-radio {
      font-size: 16px;
      cursor:pointer;
      width: 100%;
      margin: 0;
      input.n-radio[type=radio]:checked + span {
        background-position: left center;
      }
      span.n-radio {
        height: 20px;
        width: 20px;
        display: inline-block;
        overflow: hidden;
        background-image: url('<%= asset_path('ui/radiobutton_100.png') %>');
        background-size: 40px 20px;
        background-repeat: no-repeat;
        background-position: right center;
        position: relative;
        top: 4px;
        margin-right: 10px;
      }
      input.n-radio {
        display: none;
      }
    }
  }
}
// hide individual benefits continue button in employee match_person
.right-section #new_person input[type=submit] {
  display: none;
}

#showhidebutton {
  font-family: 'Open Sans', sans-serif;
  background: none!important;
  border: none;
  padding: 0!important;
  color: #069;
  cursor: pointer;
  outline:0;
  float: right;
}

.gi-2x{font-size: 2.0em;}

// disables the click action on any element; more general than :disabled
// Used to implement readonly permissions for admin
.blocking {
    opacity: 0.5 !important;
    pointer-events: none;
    cursor: default;
}
.read_only_dropdown {
  cursor: not-allowed !important;
  pointer-events: none;
}
