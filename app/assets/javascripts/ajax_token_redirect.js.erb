$( document ).ajaxComplete(function( token_expired ) {
    const root_path = JSON.parse(token_expired);
    if(root_path.length == 1) {
        window.location.assign('root_path.token_expired');
    }
  });