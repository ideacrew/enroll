%eligibility_event
  %event_kind= xml_eligibility_event_uri(eligibility_event)
  - if eligibility_event.eligibility_event_has_date?
    %event_date= simple_date_for(eligibility_event.eligibility_event_date)
