<div class="col-xs-12 add-employee-tab">
<%= form_for @census_employee, url: employers_employer_profile_census_employees_path(employer_profile_id: @employer_profile.id) do |f| %>
  <%= render 'form', f: f %>
<% end %>
</div>
