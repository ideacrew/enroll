<% if current_user && current_user.needs_to_provide_security_questions? %>

  <%= render 'users/security_question_responses/edit_modal', { action: 'create', method: 'POST' } %>

  <script>
    $('#securityQuestionModal').modal({
      backdrop: 'static',
      keyboard: false
    });
    $('.modal-backdrop').css({height: '800px'});
  </script>
<% end %>
