<%= text_field_tag cell.name_attribute, nil, placeholder: cell.placeholder, required: cell.required, class: "put-class-name-here", value: input_text_value(@model, cell) %>

<!--TODO : move this to assets-->
  <script>
  $( function() {
    $( "#applicant_pregnancy_due_on" ).datepicker();
    $( "#applicant_pregnancy_end_on" ).datepicker();
    $( "#applicant_student_status_end_on" ).datepicker();

    $( "#income_start_on" ).datepicker();
    $( "#income_end_on" ).datepicker();

    $( "#benefit_enrolled_start_on" ).datepicker();
    $( "#benefit_enrolled_end_on" ).datepicker();
    $( "#benefit_eligible_start_on" ).datepicker();
    $( "#benefit_eligible_end_on" ).datepicker();

    $( "#deduction_start_on" ).datepicker();
    $( "#deduction_end_on" ).datepicker();
  } );
  </script>