  <%= radio_button_tag "attributes[#{cell.model}][#{cell.attribute}]", cell.value, cell.checked, class: "put-class-name-here", id: cell.id %>

  <%= label_tag cell.id, cell.label,class: "put-class-name-here" %>