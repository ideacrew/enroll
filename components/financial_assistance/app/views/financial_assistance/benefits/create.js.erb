<% selector_id = @benefit_kind == "is_eligible" ? "eligible-benefit-kinds" : "enrolled-benefit-kinds" %>
stopEditing()
<%= render partial: 'replace', locals: { id: "##{selector_id} .benefits-list .#{@benefit_insurance_kind}" } %>
