<% selector_id = kind == "is_eligible" ? "eligible-benefit-kinds" : "enrolled-benefit-kinds" %>
stopEditing()
<%= render partial: 'replace', locals: { id: "##{selector_id} .benefits-list .#{insurance_kind}", kind: kind, insurance_kind: insurance_kind } %>
