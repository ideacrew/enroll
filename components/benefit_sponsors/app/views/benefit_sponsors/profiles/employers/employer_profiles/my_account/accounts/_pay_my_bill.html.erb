<h3 class="darkblue no-buffer">Billing</h3>
<h4 class="starter">
  <%= Settings.site.short_name %> issues one monthly invoice at the beginning of each month. The invoice includes next month’s premium payments, and any adjustments from previous months. The total amount due reflects all premiums due (health and dental if applicable), even when you offer coverage from more than one insurer.
</h4>
<h4>Your
  <%= Settings.site.short_name %>
  account number:
  <strong><%= "#{@employer_profile.hbx_id}".upcase %></strong>
</h4>
<h3><%= Settings.site.short_name %> offers multiple payment options including:</h3>
<h4>
  <ol>
    <li>
      <% if @wf_url.present? %>
        <span class="btn btn-default" data-target="#payOnlineConfirmation_<%= @employer_profile.id %>" data-toggle="modal">Pay Online</span> <i class="fa fa-info-circle" data-toggle="tooltip" data-placement="right" title="Fastest payment option - processed in 2-3 business days."></i>
      <% else %>
      <div>
        <button class="btn btn-default disabled">Pay Online unavailable</button> <i class="fa fa-info-circle" data-toggle="tooltip" data-placement="right" title="Fastest payment option - processed in 2-3 business days."></i>

      </div>
      <% end %>
      <div class="row col-md-offset-1">
        <%= link_to 'How to Pay Online - Instructions', asset_path("pdf/employers_paying_your_premiums_online.pdf") , target: "_blank"%>
      </div>
    </li>
    <li>Pay by Phone
      <div class="row col-md-offset-1">
        <%=  Settings.contact_center.alt_phone_number %>, select "Small Business Employer" then "Make a Payment"
      </div>
    </li>
    <li>Pay by Mail <i class="fa fa-info-circle" data-toggle="tooltip" data-placement="right" title="Slowest payment option – takes up to 2 weeks to process."></i>
      <div class="row col-md-offset-1">
        <%= contact_center_mailing_address_name %>
      </div>
      <div class="row col-md-offset-1">
        <%= contact_center_address_one %>
      </div>
      <div class="row col-md-offset-1">
        <%= contact_center_city %>,&nbsp;<%= contact_center_state %> <%= contact_center_postal_code %>
      </div>
    </li>
  </ol>
</h4>
<h4>**Please include your account number on your check.</h4>
<h4>Check that your mailing address is up to date - click on
  <%= link_to 'Update Business Info', edit_profiles_registration_path(@employer_profile.id, :tab=>'profile', add_staff: 'no') %>.
</h4>
