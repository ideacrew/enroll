{
  "draw" : <%= @draw %>,
  "recordsTotal" : <%= @total_records %>,
  "recordsFiltered" : <%= @records_filtered %>,
  "data" : [
<% @employer_profiles.each_with_index do |ep, idx| %>
  <%= render :partial => "benefit_sponsors/profiles/broker_agencies/broker_agency_profiles/employer_datatable_entry", :object => ep %>
  <%= (idx == @employer_profiles.length - 1) ? "" : "," %>
<% end %>
  ]
}
