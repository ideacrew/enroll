import { TerminateWithEarlierDate } from 'legacy/view_enrollment_to_update_end_date';

describe("legacy/view_enrollment_to_update_end_date", () => {
  test("exports the global methods", () => {
    expect(TerminateWithEarlierDate).not.toBe(null);
  });
});