default: --format pretty --fail-fast --retry 2 --order random --strict --tags 'not @wip and not @broken' -r features --profile semaphoreci
everything: --format pretty --strict -r features
semaphoreci: -q
