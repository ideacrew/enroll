$(document).ready(function () {
   $(document).on('click', '.interaction-click-control-save---exit', function(){
   	    if ($('.employee_roles #new_person_wrapper').length) {
   	       $('#exit_after_method').val('true');
   	       $('#btn-continue').click();
        } else if ($('.consumer_roles .edit_person').length) {
          $('#exit_after_method').val('true');
   	      $('.interaction-click-control-continue').click();
        }
        else {
   	    	$('.interaction-click-control-logout').click();
   	    }
   })
});




